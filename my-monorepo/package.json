{
  "name": "my-monorepo",
  "private": true,
  "workspaces": [
    "apps/*",
    "libs/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "vercel-build": "npm --prefix apps/web ci && npm --prefix apps/web run build",
    "lint": "turbo run lint",
    "test": "turbo run test"
  },
  "devDependencies": {
    "turbo": "^1.0.0",
    "typescript": "^4.0.0",
    "eslint": "^7.0.0",
    "prettier": "^2.0.0"
  },
  "dependencies": {
    "prisma": "^3.0.0"
  },
  "pnpm": {
    "packageExtensions": {
      "next@*": {
        "dependencies": {
          "baseline-browser-mapping": "2.9.18"
        }
      }
    }
  }
}
